import 'package:flutter/material.dart';

// TODO: encapusulate all styles info themeData
final themeData = new ThemeData(
  // brightness: Brightness.dark,
  backgroundColor: Color.fromARGB(200, 30, 36, 44),
  primaryColor: Colors.blueGrey,
  accentColor: Colors.white,
  textTheme: TextTheme(
    display1: TextStyle(
      color: Colors.white,
      fontFamily: 'FiraCode',
    ),
  ),
);
